# Provisioning home network

A series of scripts to provision different unix machines on my home network. To do so, the machine uses ansible.

## Setup
Run `./scripts/setup.sh`

### Pre-requisites
- `pyenv`
